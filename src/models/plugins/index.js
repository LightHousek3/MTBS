const toJSON = require('./toJSON.plugin');
const paginate = require('./paginate.plugin');
const softDelete = require('./softDelete.plugin');

module.exports = {
    toJSON,
    paginate,
    softDelete,
};